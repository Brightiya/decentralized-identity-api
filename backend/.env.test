# backend/.env.test

NODE_ENV=test
HYBRID_MODE=true
DATABASE_URL_TEST=postgresql://pimv_user:strongpassword@localhost:5432/pimv_db

# Required for contract signer in tests (Hardhat default account #0)
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

JWT_SECRET=super-secret-dev-key-for-tests-only

# Optional: Hardhat local RPC (if not already set)
RPC_URL=http://127.0.0.1:8545